# # Aiven connection (quick start)
# spring.datasource.url=jdbc:mysql://${AIVEN_HOST}:${AIVEN_PORT}/defaultdb?useSSL=true&requireSSL=true&serverTimezone=UTC
# spring.datasource.username=${AIVEN_USERNAME}
# spring.datasource.password=${AIVEN_PASSWORD}

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # JWT
# security.jwt.secret=${JWT_SECRET}
# security.jwt.access-token-expiration-ms=3600000
# security.jwt.refresh-token-expiration-ms=604800000

# server.port=8080

# # Google Maps / Geocoding API key
# security.maps.apiKey=${GOOGLE_MAPS_API_KEY}

# Default to local profile for development
spring.profiles.active=local

# Fallback configuration (will be overridden by application-local.properties)
#spring.datasource.url=jdbc:mysql://${AIVEN_HOST:localhost}:${AIVEN_PORT:3306}/${AIVEN_DATABASE:defaultdb}?useSSL=true&requireSSL=true&serverTimezone=UTC
#spring.datasource.username=${AIVEN_USERNAME:root}
#spring.datasource.password=${AIVEN_PASSWORD:password}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
security.jwt.secret=${JWT_SECRET:default-secret-key-change-in-production}
security.jwt.access-token-expiration-ms=3600000
security.jwt.refresh-token-expiration-ms=604800000

server.port=8080

# Google Maps / Geocoding API key
security.maps.apiKey=${GOOGLE_MAPS_API_KEY:}


### **Step 3: Add to `.gitignore`**

**IMPORTANT:** Add this to `fixitnow-backend/.gitignore`:
```
# Don't commit sensitive data
src/main/resources/application-local.properties